server:
  port: ${CONFIG_SERVICE_DEV_PORT:3000} #2000 for dev, 3000 for test and 4000 for prod
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/config/dev
            - classpath:/config/test
            - classpath:/config/prod
        bootstrap: 'true'
encrypt:
  keyStore:
    alias: neptune_mfb_configserv
    location: classpath:/neptune_mfb_config_key.jks
    password: fJc0ADsc7N7DdCwE2zCWWqiOUyv8lyTBkHsHRUWa9pw
    secret: fJc0ADsc7N7DdCwE2zCWWqiOUyv8lyTBkHsHRUWa9pw
    type: jks

management:
  metrics:
    export:
      defaults:
        enabled: false
    system:
      enabled: false


#Endpoint for encrypting value: [POST] http://localhost:3000/encrypt
#Endpoint for decrypting values: [POST] http://localhost:3000/decrypt
#Endpoint for reading property file values for specific service:
#http://localhost:3000/account-service/test
#http://localhost:3000/inventory-service/test
#http://localhost:3000/nofitication-service/test
#http://localhost:3000/order-service/test